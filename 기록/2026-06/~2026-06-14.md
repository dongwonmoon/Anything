06.13
18:00 프로그래밍
- (local-first 전환) 사진을 기기 로컬에 저장
	- Draft를 임시 자산으로 취급 - (고아파일 결함 방지) 저장 성공/사진 개별 제거/새로 쓰기 시 기존 draft 삭제
	- 최종 사진은 app document storage에 저장 - morrow/records/{record-id}/photos/ 아래에 사본 (압축됨)
	- 원본은 저장하지 않음 (저장공간, 렌더링 성능, 포맷 호환성, 메모리 등)
	- 사진 저장은: 임시 정규화 => 최종 경로 복사 => 파일 검증 => SQLite 저장 순서. 
	- 기록 삭제는 DB 우선: DB 삭제 성공 후 사진 파일을 최선의 노력으로 정리, 파일 삭제만 실패해도 사용자에게는 기록 삭제 성공으로 표시. 파일 정리 실패 때문에 삭제된 기록을 다시 노출하거나 사용자에게 재시도 요구 X
	- 기록 삭제는, 추후에 논리 삭제와 cleanup queue를 운영. 가장 견고하지만, 구조가 무거움.
- Maestro TDD 습관화
	- 단점이 있는데, 너무 느리고, 환경 의존적이고, UI 변경에 취약함.
	- Maestro 실행은 내가 터미널에서 직접하는 게 나을 듯.

06.14
08:30 프로그래밍
- book_snapshot_json 분리의 필요성 제기됨
	- 기능 추가 시
		1. 같은 책의 기록을 모아서 보여주기
		2. 책별 통계/필터/검색이 중요해짐
		3. 사용자가 "내 책장"을 관리
		4. 책 정보를 수정하면 관련 기록에 일괄 반영하고 싶음
- Maestro 사진 lifecycle acceptance 준비
	- 사진 저장/재시작/사진 제거/기록 삭제에 대한 사용자 flow를 Maestro로 자동화한다
	- 목적은,
		1. 사용자 데이터 안전성 확인 (앱 재시작 후에도 기록과 사진 유지)
		2. 삭제/수정의 정합성 확인 (화면과 파일의 동일화를 목표로)
		3. 개발비용 절감 (Codex 직접 QA 방지)
	- dev-client emulator만 완료. Preview/release APK는 따로 해줘야함.
- Home / 다시 보기(resurface)를 local SQLite 기준으로 전환하기